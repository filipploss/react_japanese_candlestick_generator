## React/Redux japanese candlestick chart generator

Libraries used in the app: React/Redux, Apex Charts

In the project directory, you can run `npm start` to install all the dependencies and to run the app.
